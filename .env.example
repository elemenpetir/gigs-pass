# Docker Compose (root) — dipakai saat build frontend container
# Vite embed nilai ini ke bundle saat build (bukan runtime).
# Lokal/Opsi B (Nginx reverse proxy di host): /api
# Tanpa reverse proxy: http://<host>:5000/api
VITE_API_BASE_URL=/api
